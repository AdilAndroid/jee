<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<!-- Mapping file autogenerated by MyEclipse Persistence Tools -->
<hibernate-mapping>

<!-- 
# category 代码
DROP TABLE IF EXISTS category;
CREATE TABLE IF NOT EXISTS category (	
	id BIGINT NOT NULL PRIMARY KEY,
	category_name VARCHAR(15),
	category_id BIGINT NOT NULL
);

ALTER TABLE category ADD INDEX idex_category_id(category_id),
	ADD CONSTRAINT fk_category_id FOREIGN KEY (category_id) REFERENCES category(id);

 -->
	<class name="com.freesoft.bean.Category" table="category" catalog="hibernate">
		<id name="id" type="long" column="id">
			<generator class="increment" />
		</id>
		<property name="name" type="string">
			<column name="category_name" length="15" />
		</property>

		<set name="child" cascade="all">
			<key column="category_id"></key>
			<one-to-many class="com.freesoft.bean.Category" />
		</set>
		
		<many-to-one name="parent" column="category_id" class="com.freesoft.bean.Category"></many-to-one>
	</class>
</hibernate-mapping>
